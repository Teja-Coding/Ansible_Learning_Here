- name: learning ansible loops from here
  hosts: local
  connection: local
  tasks:
  - name: print names
    ansible.builtin.debug:
      msg: "hello {{ item }}" # item is reserved key word
    loop:
    - sri
    - yuva 
    - teja 
    - manikanta